# Cross-Document Review — 2026-05-17

**Purpose:** Audit of all system specification documents against decisions made in recent sessions. Covers FRAME-SPEC.md, ROLE-CODEBOOK.md, STANDARD-FIELDS.md, OPEN-QUESTIONS.md, TRIG-DESIGN.md, and FEATURES.md. Each issue is listed with its document, location, what it currently says, what it should say, and why.

**Session decisions this review draws on:** OQ-1 (tag = 1pa), OQ-2 (epoch = 2000-01-01), OQ-5 (big-endian), OQ-7 (DOMAIN=11 active design), OQ-11 (FLAGS4 resolved), OQ-20 (tag dispatch + 1pf tag), OQ-24 (DATA_SOURCE=11 anonymous), OQ-31 (ROLE_TYPE 2-bit, Option A), OQ-32 (TRIG spec complete), form SUBMIT_ACTION=11 (anonymous pickup), date_end/expiry_date swap, no migration needed (pre-release app).

---

## 1. FRAME-SPEC.md

### F-1 — Document header date (cosmetic)
**Location:** Line 3  
**Current:** `**Status:** v0.1 draft — 2026-05-15`  
**Should be:** `**Status:** v0.2 draft — 2026-05-17`

---

### F-2 — Codebook tag TBD (blocking)
**Location:** Section 1, Encoding Envelope  
**Current:** `**Codebook tag:** TBD (new tag distinct from 1eg and 1bg; signals pads-v1 format to receiver)`  
**Should be:** `**Codebook tag:** \`1pa\` — pads v1, package a. Resolved OQ-1.`  
**Why:** OQ-1 resolved. Tag is 1pa.

---

### F-3 — COMPACT_TIME epoch wrong throughout (blocking)
**Location:** Section 2 (meta2 bit 6 description), Section 6 (field mapping table)  
**Current:** `days since 2020-01-01`  
**Should be:** `days since 2000-01-01`  
**Why:** OQ-2 resolved — user switched from 2020 to 2000 to cover historical job dates. The change is a codec constant (not stored in frame) — zero byte cost. Also needs fixing in Frame Profile C commentary and the Section 6 migration table.

---

### F-4 — DOMAIN=11 described as reserved (wrong)
**Location:** Section 2, meta2 bits 3-2  
**Current:** `11=reserved`  
**Should be:** `11=active design pending (hybrid I>O + BitLedger Account Pair; decoder treats as not-yet-implemented, OQ-7)`  
**Why:** User explicitly said "we'll design 11 soon" — it is not reserved/error, it is in active design. A decoder receiving DOMAIN=11 should gracefully degrade, not hard-reject.

---

### F-5 — uint16 length prefix endianness wrong (blocking)
**Location:** Data blocks section, encoding note  
**Current:** `text (standard): [uint16 length LE][UTF-8 bytes]`  
**Should be:** `text (standard): [uint16 length BE][UTF-8 bytes]`  
**Why:** OQ-5 resolved — big-endian throughout ALL multi-byte integers, including text length prefixes. The "LE" note contradicts the settled decision. STANDARD-FIELDS.md also needs this check (see SF-4 below).

---

### F-6 — Section 6 Migration Path is obsolete (cleanup)
**Location:** Section 6, entire section  
**Current:** Full migration strategy for 1eg/ → pads-v1 with Phase 1/2/3 plan and field mapping table  
**Should be:** Replaced with a single short paragraph: app is pre-release (private demo only), no migration path needed, 1eg/ is replaced entirely, existing records are test data.  
**Why:** User explicitly said "please disregard any considerations about legacy connections etc for the whole app, it's only been in private demo mode." Keeping the migration section is misleading — it implies a live user base to migrate.

---

### F-7 — Section 7 Open Design Points — multiple stale entries
**Location:** Section 7 table (appears after Section 15 in the file — numbering is also out of order)  
**Issues in the table:**

| Current text | Correct status | Fix |
|---|---|---|
| "Codebook tag string: New tag to be chosen" | ✓ Resolved | 1pa (OQ-1) |
| "TAX_CODE=01/10 codebook values: defined per codebook package" | ✓ Resolved | No rates in codebook — always explicit. No percentages for any currency. (OQ-4) |
| "DOMAIN=11 (hybrid): Reserved; decoder must reject" | Active design | Decoder gracefully degrades, does not reject (OQ-7) |
| "uint16 length prefix byte order: Little-endian...vs big-endian — standardise at implementation" | ✓ Resolved | Big-endian throughout (OQ-5) |
| "Epoch for COMPACT_TIME dates: 2020-01-01 proposed; finalise at implementation" | ✓ Resolved | 2000-01-01 (OQ-2) |

Also: the section is numbered §7 but appears after §15 in the file. Suggest renaming to §16 or moving it to the top of the file as a status/decisions summary.

---

### F-8 — Section 9 State Commit — stale pending note
**Location:** Section 9, "Date range for period records" note  
**Current:** `*Note: field_flags3 bit 3 currently assigned expiry_date; resolution pending — swap expiry_date to FLAGS4, reassign bit 3 to date_end`  
**Should be:** `Note: expiry_date moved to field_flags4 bit 1 (financial template). field_flags3 bit 3 = date_end. Resolved.`  
**Why:** This was resolved — FRAME-SPEC section 2 already shows field_flags3 bit 3 as date_end. The note in section 9 contradicts the resolved definition in section 2.

---

### F-9 — Section 11.1 DATA_SOURCE=11 wrong (semantic error)
**Location:** Section 11.1 Display Schema, DATA_SOURCE bits  
**Current:** `11=hybrid (some inline, some resolved by receiver)`  
**Should be:** `11=anonymous/stealth (no sender identity in payload; no reply routing address; SUBMIT_ACTION=11 required if form present; shell shows placeholder text only — OQ-24)`  
**Why:** OQ-24 resolved with anonymous/stealth as the fourth data_source mode. "Hybrid" was the original placeholder. The anonymous mode is a distinct design feature.

---

### F-10 — Section 11.1 Display Schema — TRIG block entirely missing (major gap)
**Location:** Section 11.1, after display_flags2 block  
**Current:** Section ends after display_flags2; no TRIG block defined  
**Should add:** A TRIG flag in the DISPLAY_CONTROL byte or display_flags2 byte, plus the TRIG block wire format.

Proposed addition to display_flags2:
```
  bit 2: HAS_TRIG    1=TRIG bytecode block follows display_schema
```

New block definition after display_flags2:
```
[trig_block]           variable — if HAS_TRIG=1
  [trig_len]           1 byte — uint8 length of TRIG program (0–20)
  [trig_bytes]         N bytes — TRIG v1 bytecode; see OQ-32 for full spec
  If trig_len=0: block present but empty; shell defaults to SHOW_ALWAYS NATIVE.
  If trig_len>20: spec error; shell renders BLANK.
```

**Why:** OQ-32 TRIG spec is complete. The wire format location for TRIG bytes must be defined in FRAME-SPEC. Currently display_flags2 bits 2-0 are reserved — bit 2 is the natural slot for HAS_TRIG. Also note: OQ-32c asks whether TRIG should instead have its own block presence bit in meta2 — this question should be answered before the edit is made (see Open Items at the bottom of this review).

---

### F-11 — Section 11.2 Form Schema SUBMIT_ACTION=11 wrong (semantic error)
**Location:** Section 11.2 Form Schema  
**Current:** `11=reserved`  
**Should be:** `11=anonymous pickup (submissions held server-side; sender retrieves via blind pickup code derived from master_secret + form UID; no routing address in payload — requires DATA_SOURCE=11)`  
**Why:** SUBMIT_ACTION=11 was resolved as part of the anonymous/stealth mode design in OQ-24.

---

### F-12 — Section 12 Security Wrapper — URL tags wrong and incomplete (blocking)
**Location:** Section 12, URL tags block  
**Issues:**
1. `#1pv/<payload>` is listed for "plaintext" — the correct tag is `#1pa/`
2. `#1pf/` tag (financial presentation) is missing entirely
3. `#l/` tag (list share) is missing
4. The tag table should be the canonical reference; currently it is partial and has an error

**Full correct tag dispatch table:**

| Tag | Name | Purpose |
|-----|------|---------|
| `#1pa/` | Plain record | Standard records (financial, service, contact) — no presentation wrapper |
| `#1pb/` | Public billboard | Non-financial presentation: cards, service menus, contact forms. Public circulation. Never carries financial data. |
| `#1pf/` | Financial presentation | Invoice display, statements, pay summaries. Extra share-sheet safety. Not for public circulation. |
| `#1ps/` | Full scramble | AES-CTR encryption + field scramble. Private/colleague records, cost data. |
| `#1ph/` | Partial scramble | Header bytes plain (meta1-transaction_byte); field data and financial block encrypted. |
| `#1pt/` | Template-keyed | Template content is encryption key. Template ID plain; meaningless without template. |
| `#l/` | List share | Standalone option list for form builder. Not a record — list content only. |

---

### F-13 — Section 13 alt_id — "pending" language not updated
**Location:** Section 13, opening paragraphs  
**Current:** `"Resolution options (pending OQ-31 decision): Option A... Option B..."` and `"Until resolved, alt_id is treated as post-MVP."`  
**Should be:** `"Resolved (OQ-31): Option A confirmed. ROLE_TYPE packed into 2 bits, freeing bit 4 for HAS_ALT_ID. ROLE-CODEBOOK.md updated to reflect 2-bit quick-select."`  
**Why:** User confirmed Option A. The "pending" language is stale and the section already shows the Option A part_flags layout — it just contradicts itself in the preamble.

---

### F-14 — Section 15 Block Presence Summary — TRIG row missing
**Location:** Section 15 table  
**Missing row:** TRIG block  
**Should add:** `| TRIG block (within display_schema) | cond | cond | cond | — | — |`  
(present whenever display_schema is present AND HAS_TRIG=1)

**Also:** The `form_schema` row shows `cond | — | — | — | —` (only Service template can have forms). Since `#1pf/` financial presentation records and contact cards can also have forms (e.g. booking form attached to a financial record, contact request form), this should be `cond | cond | cond | — | —`.

---

### F-15 — Section numbering out of order (cosmetic)
**Location:** After Section 15, "## 7. Open Design Points" appears  
**Issue:** Section 7 appears after section 15. This is clearly a historical artefact from sections being added out of order.  
**Fix:** Renumber to §16 or restructure to place status/decisions at the top.

---

## 2. ROLE-CODEBOOK.md

### R-1 — ROLE_TYPE still shows 3-bit (major discrepancy)
**Location:** Section 1.1 header and table  
**Current:** `### 1.1 ROLE_TYPE 3-bit quick-select (part_flags bits 6–4)` with 7 named roles (000–110) plus extended (111)  
**Should be:** `### 1.1 ROLE_TYPE 2-bit quick-select (part_flags bits 6–5)` with 3 named roles (00–10) plus extended (11)

The correct 2-bit table:
| Code | Value | UI Label |
|------|-------|----------|
| `00` | 0 | Customer / Client |
| `01` | 1 | Worker / Tradesperson |
| `10` | 2 | Supplier / Vendor |
| `11` | 3 | **Extended** — role_code follows |

Roles now via role_code path (were previously in quick-select): Subcontractor (`0x50`-range in construction/services), Employee (Group 0 Management, any employed role), Agent (Group 1/8), Authority/Inspector (Group C Government).

**Why:** OQ-31 resolved as Option A — ROLE_TYPE packed into 2 bits to free bit 4 of part_flags for HAS_ALT_ID. FRAME-SPEC.md already reflects the 2-bit layout (bits 6-5, not 6-4). ROLE-CODEBOOK.md has not been updated.

**Cascading fixes needed in ROLE-CODEBOOK.md:**
- Section 1.1: header + table → 2-bit
- Section 4 (Encoding Decision D20): `"ROLE_TYPE 3-bit quick-select retained"` → `"ROLE_TYPE 2-bit quick-select (OQ-31, Option A). Subcontractor, Employee, Agent, Authority all via role_code path."`
- Section 5 (ROLE_TYPE mapping table): remove rows for Subcontractor, Employee, Agent, Authority (they are no longer quick-select)

---

### R-2 — Wire format description still says bits 6-4
**Location:** Section 1.1, sub-heading  
**Current:** `part_flags bits 6–4`  
**Should be:** `part_flags bits 6–5`  
**Why:** 2-bit field now lives in bits 6-5 (not 6-4), per OQ-31 Option A resolution.

---

### R-3 — Section 4 Decision text references old 3-bit decision
**Location:** Section 4, opening: `**Decision:** ROLE_TYPE 3-bit quick-select retained.`  
**Should be:** `**Decision (D20, updated by OQ-31 Option A):** ROLE_TYPE reduced to 2-bit quick-select. Subcontractor, Employee, Agent, and Authority/Inspector now use the role_code extended path. Bit 4 of part_flags freed for HAS_ALT_ID.`

---

## 3. STANDARD-FIELDS.md

### SF-1 — COMPACT_TIME epoch wrong (blocking)
**Location:** Section 2.1 bit 2 (date field note), Section 9 encoding summary  
**Current:** `days since 2025-01-01` (appears twice)  
**Should be:** `days since 2000-01-01`  
**Why:** OQ-2 resolved. Epoch is 2000-01-01. The STANDARD-FIELDS.md has a different wrong epoch (2025, not 2020) — both are wrong.

---

### SF-2 — FLAGS3 bit 3 still shows expiry_date (wrong)
**Location:** Section 3, FLAGS3 table, bit 3  
**Current:** `| 3 | expiry_date | [u16 days] (COMPACT) or [u16 len][UTF-8] | Quote or offer expiry date |`  
**Should be:** `| 3 | date_end | [u16 days] (COMPACT) or [u16 len][UTF-8] | Period end date — for reports, summaries, and range records |`  
**Why:** expiry_date was moved to field_flags4 bit 1 (financial template); FLAGS3 bit 3 is now date_end. This was resolved and reflected in FRAME-SPEC.md but STANDARD-FIELDS.md was not updated.

---

### SF-3 — SELF_DESCRIBING index 0x13 maps to expiry_date (wrong)
**Location:** Section 8, SELF_DESCRIBING Field-Name Index table  
**Current:** `| 0x13 | expiry_date | FLAGS3 bit 3 |`  
**Should be:** `| 0x13 | date_end | FLAGS3 bit 3 |`  
**Why:** Same swap as SF-2. The index table maps index → canonical name → field flag. index 0x13 was the expiry_date slot; it is now date_end.

---

### SF-4 — FLAGS4 section missing financial template assignments
**Location:** Section 4, FLAGS4 domain-specific  
**Current:** Lists Template 011 (Contact/entity) bits 0-4 and Template 101 (State Commit) bits 0-2, but does NOT list financial template (BASE_TEMPLATE=001) assignments  
**Should add:**
```
- Template 001 (Financial record): FLAGS4 bit 0 = service_ref (back-reference to generating service template, [u8 len][UTF-8]); bit 1 = expiry_date (moved from FLAGS3 bit 3, u16 days*)
```
**Why:** expiry_date moved from FLAGS3 bit 3 to FLAGS4 bit 1 for financial records; service_ref was added at FLAGS4 bit 0. These assignments exist in FRAME-SPEC.md section 14 but are absent from STANDARD-FIELDS.md.

---

### SF-5 — "Backwards compatibility" design principle references pads-v2-encoding-spec
**Location:** Section 1, Design Principle 5  
**Current:** `"Bits 0-11 match the pads-v2-encoding-spec.md field assignments exactly. Existing encoders/decoders need no changes for the base field set."`  
**Consider:** Whether this backward compatibility framing is still accurate given the no-migration decision (F-6 above). If pads-v2-encoding-spec.md itself is superseded by pads-v1/1pa, this note is confusing. At minimum it should reference 1pa specifically.

---

## 4. OPEN-QUESTIONS.md

### OQ-1 — Summary table entry for OQ-32 correct but should link to spec
**Location:** Summary table, OQ-32 row  
**Current:** Entry present and marked `✓ SPEC COMPLETE`  
**No change needed to content** — spec is accurate.  
**Minor note:** The OQ-32 section is now very long (the full spec). The summary table entry should remain short and just reference the section. Currently it does — no edit required.

---

### OQ-2 — Epoch note in OQ-2 section
**Location:** OQ-2 section  
**Current:** `**Decision:** 2000-01-01.` — correct ✓  
**No change needed.**

---

### OQ-3 — Currency table references 0x00–0x7F as ISO 4217 aligned but the sub-table maps GBP=0x01, USD=0x02 etc
**Location:** FRAME-SPEC.md Section 2 currency_ext note says `0x00–0x7F: ISO 4217 aligned (GBP=0x01, USD=0x02, EUR=0x03, NGN=0x20, KES=0x21...)` but the detailed table in OQ-3 of OPEN-QUESTIONS.md uses 0x01–0x0F for major currencies and 0x20–0x4F for Africa.  
**Issue:** GBP=0x03 in the OQ-3 table, but the frame spec note says GBP=0x01. Need to ensure the two documents agree.  
**Resolution:** The OQ-3 table in OPEN-QUESTIONS.md is authoritative (it was explicitly decided). FRAME-SPEC.md currency_ext note should reference OQ-3 rather than listing specific codes inline.

---

## 5. TRIG-DESIGN.md

### T-1 — Part 10 checklist is scattered across Notes A, B, C (consolidation opportunity)
**Location:** End of Part 10, and ends of Notes A, B, C  
**Current:** Three separate "New checklist items for Part 10" appendices after each note  
**Suggested:** Consolidate all checklist items into Part 10 proper. The three notes added 11 new items but they are appended separately and don't feel unified.  
**Priority:** Low — functional as-is, consolidation is cosmetic.

---

### T-2 — OQ-32c: TRIG block location not yet resolved
**Location:** OQ-32 sub-questions, OQ-32c  
**Current question:** "Should TRIG get its own block presence bit in meta2 (for records that carry TRIG but no other display schema)?"  
**Status:** Unresolved — this affects the FRAME-SPEC edit for F-10 above.  
**Impact:** If TRIG gets a meta2 bit, then `#1pa/` records (plain financial/service) could carry TRIG bytes without having a display_schema block. If TRIG stays inside display_schema, then only `#1pb/`/`#1pf/` records can carry TRIG.  
**Recommendation before editing FRAME-SPEC:** Decide this now. Most likely answer: TRIG stays inside display_schema for MVP (it is a presentation tool; plain records don't need it). Meta2 bit reserved for future.

---

## 6. FEATURES.md

### FE-1 — TRIG entry updated (✓ current)
The TRIG entry was updated in this session with the full spec summary. No further changes needed.

---

### FE-2 — Derived passphrase system references "per-contact code storage" in Features
**Location:** Features.md, Derived passphrase system entry  
**Current:** Describes master_secret + HMAC derivation correctly  
**Note:** The `passphrase_for()` function shows `period` counter but doesn't reference the `recovery QR` mechanism described in detail. This is fine — the Features entry is a summary and OPEN-QUESTIONS.md has the full design.

---

## 7. Cross-Document Consistency Issues

### X-1 — "days since epoch" appears as three different values across docs
| Document | Current value | Correct value |
|----------|--------------|---------------|
| FRAME-SPEC.md Section 2 | `2020-01-01` | `2000-01-01` |
| FRAME-SPEC.md Section 6 (migration table) | `2020-01-01` | n/a (section to be removed) |
| STANDARD-FIELDS.md Section 2.1 | `2025-01-01` | `2000-01-01` |
| STANDARD-FIELDS.md Section 9 | `2025-01-01` | `2000-01-01` |
| OPEN-QUESTIONS.md OQ-2 | `2000-01-01` | ✓ correct |
| BLOCK-DECISIONS.md | (not checked) | should match OQ-2 |

Single authoritative value: **2000-01-01**. All documents should match OQ-2.

---

### X-2 — ROLE_TYPE bit count contradicts across docs
| Document | Says | Correct |
|----------|------|---------|
| FRAME-SPEC.md §2 part_flags | `bits 6-5: ROLE_TYPE` (2 bits) | ✓ correct |
| FRAME-SPEC.md §13 alt_id block | Shows correct 2-bit layout | ✓ correct |
| ROLE-CODEBOOK.md §1.1 | `bits 6-4` (3 bits), 7 quick-select roles | ✗ outdated |
| ROLE-CODEBOOK.md §4 | "3-bit quick-select retained" | ✗ outdated |

---

### X-3 — FLAGS3 bit 3 name contradicts across docs
| Document | Says | Correct |
|----------|------|---------|
| FRAME-SPEC.md §2 field_flags3 | `date_end` | ✓ correct |
| FRAME-SPEC.md §14 FLAGS4 | `expiry_date` at FLAGS4 bit 1 | ✓ correct |
| STANDARD-FIELDS.md §3 | `expiry_date` at FLAGS3 bit 3 | ✗ outdated |
| STANDARD-FIELDS.md §8 (index 0x13) | `expiry_date` | ✗ outdated |

---

### X-4 — URL tag "#1pa" vs "#1pv" inconsistency
| Document | Says | Correct |
|----------|------|---------|
| FRAME-SPEC.md §12 | `#1pv/` for plain records | ✗ wrong — should be `#1pa/` |
| OPEN-QUESTIONS.md OQ-20 | `#1pa/` | ✓ correct |
| FEATURES.md | various — uses `1pa` | ✓ correct |

---

## 8. Priority Edit Order

If editing documents one at a time, suggested priority:

**Tier 1 — Blocking errors (must fix before any codec work):**
1. STANDARD-FIELDS.md: epoch 2025-01-01 → 2000-01-01 (SF-1) — blocks any COMPACT_TIME implementation
2. FRAME-SPEC.md: codebook tag TBD → 1pa (F-2) — blocks any URL generation
3. FRAME-SPEC.md: epoch 2020-01-01 → 2000-01-01 (F-3) — same block
4. FRAME-SPEC.md: endianness LE → BE (F-5) — blocks correct frame parsing
5. FRAME-SPEC.md: security wrapper #1pv → #1pa (F-12) — blocks URL dispatch

**Tier 2 — Semantic errors (wrong meaning, confusing for implementers):**
6. FRAME-SPEC.md: DOMAIN=11 reserved → active design pending (F-4)
7. FRAME-SPEC.md: DATA_SOURCE=11 hybrid → anonymous/stealth (F-9)
8. FRAME-SPEC.md: SUBMIT_ACTION=11 reserved → anonymous pickup (F-11)
9. STANDARD-FIELDS.md: FLAGS3 bit 3 expiry_date → date_end (SF-2 + SF-3)
10. STANDARD-FIELDS.md: FLAGS4 financial template assignments missing (SF-4)
11. ROLE-CODEBOOK.md: ROLE_TYPE 3-bit → 2-bit throughout (R-1, R-2, R-3)

**Tier 3 — Completeness gaps (missing features):**
12. FRAME-SPEC.md: Add TRIG block to display_schema (F-10) — after resolving OQ-32c
13. FRAME-SPEC.md: Add full URL tag dispatch table (F-12 second part)
14. FRAME-SPEC.md: Add #1pf/ documentation to Section 11

**Tier 4 — Stale/cleanup:**
15. FRAME-SPEC.md: Remove migration section (F-6)
16. FRAME-SPEC.md: Update Open Design Points table (F-7)
17. FRAME-SPEC.md: State Commit pending note (F-8)
18. FRAME-SPEC.md: alt_id pending language (F-13)
19. FRAME-SPEC.md: Add TRIG row to block presence summary (F-14)
20. FRAME-SPEC.md: Fix section numbering (F-15)

---

## 9. Open Items Before Editing (Decision Required)

**OQ-32c — TRIG block location:** Does TRIG live inside display_schema (HAS_TRIG flag in display_flags2) or does it get its own block presence bit in meta2?
- **Recommendation:** Keep inside display_schema for MVP. Plain `#1pa/` records have no presentation concerns. Reservation of a meta2 bit for TRIG can happen in a future version. This unblocks F-10.

**OQ-3 / X-1 inconsistency in currency codes:** FRAME-SPEC.md inline note says `GBP=0x01` but OQ-3 table says `GBP=0x03`. The OQ-3 table should be authoritative. FRAME-SPEC.md should drop the inline examples and just reference OQ-3 (which will eventually become its own CURRENCY-CODEBOOK.md). This also needs resolving before codec work.

**Section 7 / §16 placement:** Should the Open Design Points section move to the top as a decisions summary, or stay at the bottom as an appendix? Recommend moving to top (or creating a separate STATUS.md) as it's the first thing an implementer wants to see.
